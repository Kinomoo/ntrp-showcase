本檔說明本 Showcase 倉庫 的安全邊界與使用限制，避免讀者誤將此專案視為可直接上線的完整系統。

# 目的與範圍
- 本倉庫僅用於公開展示：架構說明、功能截圖、以及少量代表性程式碼片段。
- 不含任何可直接重建服務的關鍵資訊；實際營運程式碼、環境設定與部署腳本皆在私有倉庫。

# 刻意不包含的內容
- 憑證／金鑰／密碼（例如：DISCORD_CLIENT_SECRET、資料庫連線字串）。
- 完整商業邏輯（含所有授權規則細節、錯誤復原策略）。
- 內部資料表完整定義與遷移腳本（Showcase 僅提供摘要片段）。
- 部署與基礎設施設定（Nginx、PM2、雲端供應商、監控與警報）。

# 憑證與環境變數政策
- .env.example 只列變數名稱，不提供任何實際值。
- 任何金鑰、Access Token、資料庫連線等機密禁止提交至版本控制（含 Git 歷史）。
- 程式碼中涉及外部服務的呼叫一律透過 process.env.* 參考，不硬編碼任何敏感資訊。

# 第三方服務與權限
- 認證採用 Discord OAuth（NextAuth），常用 Scopes：identify、email、guilds.members.read。
- 以 Discord Bot Token 取得 Guild 成員角色，據以推導權限（admin／whitelisted／donor）。
- 所有第三方 API 金鑰與 Token 僅於私有環境設定，本倉庫不提供任何實際值。

# 資料與隱私
- 本倉庫不包含任何真實使用者個資（PII）。截圖如含敏感資訊將做遮蔽或替換。
- 展示用程式碼不會在公開環境寫入真實資料庫；資料表結構僅供理解模型之用。

# 安全實作位置
- 認證／授權：NextAuth 設定、JWT／Session 加料、角色與權限映射的完整實作在私有倉庫。
- 中介層保護：正式環境內含更完整的安全性標頭、額外檢查與風險緩解邏輯。
- 資料庫操作：實際 SQL／ORM、交易控制、索引與備援策略均收錄於私有倉庫。

# DEMO
- Demo 網站平常關閉；於求職期間短暫開啟供審閱。
- Demo 僅供功能展示，不代表最終效能或完整防護配置。

# 使用限制
- 本倉庫內容僅供技術審閱與面試展示；不得用於商業或衍生產品。
- 未經授權不得複製、散布或嘗試以本倉庫內容部署服務。